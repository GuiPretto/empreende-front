import { AppBar, Toolbar } from "@material-ui/core"

const Header = () => {

    return (
        <AppBar position={'static'}>
            <Toolbar>
                EmpreendeApp
            </Toolbar>
        </AppBar>
    )
}

export default Header