import styled from "@emotion/styled";

const MapInputOption = styled.div`
    width: 100%;

    text-overflow: ellipsis;
    white-space: nowrap;
    overflow: hidden;
`

export {
    MapInputOption
}